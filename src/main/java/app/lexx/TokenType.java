package app.lexx;

public enum TokenType {
    NUM,
    ID,
    IF,
    ELSE,
    WHILE,
    DO,
    FOR,
    LBRA,
    RBRA,
    LPAR,
    RPAR,
    PLUS,
    MINUS,
    MUL,
    LESS,
    EQUAL,
    SEMICOLON,
    EOF
}
